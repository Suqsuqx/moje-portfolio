# Figma Make reference

This directory contains reference or generated prototype code from Figma Make. It is not automatically part of the production architecture. Review and migrate useful UI or animation logic incrementally into `src/`, adapting it to the app's data model and conventions.
